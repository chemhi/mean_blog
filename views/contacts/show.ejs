<!-- views/contacts/show.ejs -->

<!DOCTYPE html>
<html>
  <head>
    <% include ../partials/head %>
  </head>
  <body>
    <% include ../partials/nav %>

    <div class="contact contact-show">
      <div class="contact-menu">
        <a href="/contacts"> Back </a>
        <a href="/contacts/<%= contact_obj._id %>/edit"> Edit </a>
        <form action="/contacts/<%= contact_obj._id %>?_method=delete" method="post">
          <a href='#' onclick="confirm('Do you want to delete this?')?this.parentElement.submit():null;"> Delete </a>
        </form>
      <h2>Show</h2>
      <h3><%= contact_obj.name %></h3>
      <div>
        <label> Email </label><span><span><%= contact_obj.email %></span>
      </div>
      <div>
        <label> Phone </label><span><span><%= contact_obj.phone %></span>
      </div>
    </div>
  </body>
</html>
